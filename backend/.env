# Server
HOST=0.0.0.0
PORT=8000

# HLS — on Windows /tmp/hls doesn't exist by default, so we use a local folder
HLS_OUTPUT_DIR=./hls_output

# FFmpeg — must be in system PATH (you confirmed it is ✅)
FFMPEG_PATH=ffmpeg

# HLS tuning
HLS_SEGMENT_TIME=2
HLS_LIST_SIZE=5
