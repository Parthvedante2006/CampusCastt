import 'package:flutter/material.dart';

class ChannelOwnerDashboardScreen extends StatelessWidget {
  const ChannelOwnerDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Channel Owner Dashboard')),
      body: const Center(child: Text('Placeholder: Channel Owner Dashboard')),
    );
  }
}
